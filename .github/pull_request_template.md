## What this PR does

Brief description. What changed and why.

## Related issues

Closes #
Refs #

## How I tested it

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `npm run test:e2e` passes (if webview changes)
- [ ] `npm run test:vscode` passes (if extension activation / commands changed)
- [ ] Manually verified in VS Code via `npm run install:local`

## Notes for the reviewer

Anything tricky, anything you'd like a second opinion on, anything you decided not to do.
